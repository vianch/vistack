---
description: "Alias for the canonical viStack engineering-work entry point"
argument-hint: "<what you observed or want>. Done means <checkable condition>. Keep <behaviour that must not change>."
---

# /vistack:orchestrator

Invoke the canonical `vistack` skill at `skills/vistack/SKILL.md` and follow it as
executable instructions, starting at Step 0. This alias defines no separate workflow.

The request is `$ARGUMENTS`; preserve the canonical skill's empty-request behavior and
sticky mode.
